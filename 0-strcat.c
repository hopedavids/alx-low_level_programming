#include <stdio.h>
#include "main.h"

/**
 * _strcat - the name of the function
 * @dest: the parameter parsed into the function
 * @src: the src parameter parsed into the function
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src)
{
	

}
